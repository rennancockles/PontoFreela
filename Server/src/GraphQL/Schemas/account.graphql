type Mutation {
    updateAccount(account: AccountInput!): Account
    deleteAccount(account: AccountInput!): [Account!]
}

type Account {
    id: ID!
    name: String!
    hourlyRate: Float!
    active: Boolean!
}

input AccountInput {
    id: ID
    name: String!
    hourlyRate: Float!
    active: Boolean!
}