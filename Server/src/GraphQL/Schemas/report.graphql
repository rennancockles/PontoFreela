type Query {
    report(id: ID): Report
    reports(accountId: ID): [Report]
}

type Mutation {
    insertReport(report: ReportInput!): Report
}

type Report {
    id: ID!
    date: Date!
    obs: String
    records: [Record]
}

input ReportInput {
    accountId: ID!
    date: Date!
    obs: String
    records: [RecordInput!]!
}